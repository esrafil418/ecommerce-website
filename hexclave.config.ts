import type { HexclaveConfig } from "@hexclave/react/config";

export const config: HexclaveConfig = {
  "apps": {
    "installed": {
      "authentication": {
        "enabled": true
      }
    }
  }
};
